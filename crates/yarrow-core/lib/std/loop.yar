# Loop helpers. `value`, `index`, `break`, and `continue` are compiler
# intrinsics resolved when this module is required (e.g. `"std.loop" loop
# require`). They are not ordinary callable functions.
